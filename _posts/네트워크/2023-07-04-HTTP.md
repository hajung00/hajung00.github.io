---
layout: single
title: "HTTP 기본"
categories: network
tag: [네트워크]
toc: false
author_profile: false
sidebar:
  nav: "docs"
toc: true
toc_label: "목록"
toc_icon: "bars"
toc_sticky: true
---

## 1. HTTP

- 특징

1. 클라이언트 서버 구조

-그림 첨부

- request response 구조
- 클라이언트는 서버에 요청을 보내고, 응답을 대기
- 서버가 요청에 대한 결과를 만들어서 응답

## 2. 무상태 프로토콜(스테이스리스)

- stateful, stateless 차이

* stateful(상태 유지)
  서버가 클라이언트의 이전상태(문맥)를 보존 => 항상 같은 서버가 유지되어야함.
* 그림 첨부
  상황 a) 서버가 중간에 문제가 생김
* 그림 첨부
* 장애 발생

* stateless(무상태)
  서버가 클라이언트의 이전상태(문맥)를 보존하지 않음
  (클라이언트에서 그때 그때 필요한 데이터를 다 담아서 보내줌)
* 그림 첨부
  상황 a) 서버가 중간에 문제가 생김
* 그림 첨부
* 다른 서버로 교체하여 장애 생기지 않음.

* 차이점
  서버가 중간에 변경되었을 때, stateful에서 장애 발생 stateless는 장애 없음.

- stateless의 장단점
  장점: 서버 확장성 높음(클라이언트 요청이 증가해도 서버를 대거 투입 가능, 무한한 서버 증설 가능)
  단점: 클라이언트가 추가 데이터 전송

- stateless 실무 한계

* 모든 것을 무상태로 설계 할 수 있는 경우도 있고 없는 경우도 있다.
* 무상태 (ex) 로그인이 필요 없는 단순한 서비스 소개 화면)
* 상태 유지(ex) 로그인)
* 로그인한 사용자의 경우 로그인 했다는 상태를 서버에 유지
* 일반적으로 브라우저 쿠키와 서버 세션등을 사용해서 상태 유지
* 상태 유지는 최소한만 사용

## 3. 비연결성
